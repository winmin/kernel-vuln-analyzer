# 内核漏洞分析报告: {{BUG_ID}}

**日期**: {{DATE}}
**分析师**: Claude (AI辅助分析)
**状态**: {{STATUS}}

---

## 1. 执行摘要

{{一段话：漏洞是什么、位于哪里、严重程度、可利用性评级和修复状态。}}

| 字段 | 值 |
|---|---|
| **漏洞ID / CVE** | {{CVE或跟踪ID}} |
| **漏洞类型** | {{例如：释放后使用、越界写入}} |
| **子系统** | {{例如：net/netfilter, fs/ext4}} |
| **受影响版本** | {{例如：5.15 - 6.7}} |
| **可利用性** | {{高度可利用 / 可能 / 潜在 / 不太可能 / 不可利用}} |
| **CVSS估分** | {{如适用}} |
| **攻击面** | {{例如：非特权本地、需要CAP_NET_ADMIN}} |
| **修复状态** | {{补丁已开发并验证 / 补丁已提议 / 分析中}} |

---

## 2. 崩溃日志分析

### 2.1 原始崩溃报告

```
{{粘贴原始崩溃日志，带注释}}
```

### 2.2 关键信号

- **检测器**: {{KASAN / UBSAN / BUG / panic / GPF}}
- **漏洞子类型**: {{例如：slab-use-after-free}}
- **访问**: 在地址 {{addr}} 处{{读/写}} {{N}} 字节
- **出错函数**: {{function+offset}}
- **Slab缓存**: {{缓存名称}} (大小 {{N}})
- **任务**: {{任务名}} (PID {{pid}})
- **内核版本**: {{version}}

### 2.3 调用栈分析

```
{{带注释的调用栈，标注关键帧的说明}}
```

**子系统识别**: {{如何从调用栈确定子系统}}

---

### 2.4 子系统背景

{{在描述漏洞之前，先解释受影响子系统的正常工作方式。
本节帮助不熟悉该子系统的读者理解上下文。}}

- **架构**: {{关键数据结构、它们的关系、生命周期}}
- **正常调用路径**: {{代码在非漏洞情况下应该做什么}}
- **为何此区域容易出错**: {{例如：复杂状态机、性能关键路径验证不足、历史上类似漏洞的模式}}

---

## 3. 根因分析

### 3.1 漏洞代码路径

{{描述导致漏洞的代码路径。引用具体源文件和行号。}}

```c
// {{file}}:{{line}}
{{相关代码片段，带注释}}
```

### 3.2 漏洞机制

{{详细的时间顺序描述，说明漏洞如何触发：}}

1. **步骤1**: {{线程/上下文A做了X...}}
2. **步骤2**: {{线程/上下文B做了Y...}}
3. **步骤3**: {{不变量违规发生，因为...}}
4. **步骤4**: {{崩溃发生，当...}}

### 3.3 真实漏洞分类

| 表象 | 根因 |
|---|---|
| {{崩溃日志显示的}} | {{实际漏洞是什么}} |

{{解释为什么表象与根因不同（如适用）。
例如："崩溃表现为偏移0x40处的NULL指针解引用，但实际上是UAF——对象已被释放，
INIT_ON_FREE将内存清零，导致偏移0x40处的指针字段读为NULL。"}}

### 3.4 引入提交

```
{{hash}} ("{{commit title}}")
```

{{此提交如何引入了该漏洞。}}

---

## 4. 动态分析

### 4.1 复现

- **QEMU配置**: {{CPU、内存、内核配置要点}}
- **PoC**: {{PoC的简要描述及触发漏洞的方式}}
- **复现率**: {{例如：2 CPU时100%，1 CPU时约30%}}

### 4.2 GDB会话要点

{{GDB调试的关键发现：}}

```gdb
{{确认根因的关键GDB命令和输出}}
```

### 4.3 对象生命周期

{{如为UAF：描述 分配 → 使用 → 释放 → 释放后使用 的生命周期，附时间戳/断点}}

---

## 5. 可利用性评估

### 5.1 评级: {{RATING}}

### 5.2 权限门分析（`capable` vs `ns_capable`）

从系统调用入口到漏洞代码，追踪每个权限检查：

```
{{系统调用入口 / 数据包接收}}
         │
         ▼
{{框架层}} ({{file.c:line}})
│  {{检查函数}}({{capability}})  ← {{命名空间感知 / 仅init_user_ns}}
│  {{userns root 能/不能 通过}}
│  失败 → {{-EPERM / 丢弃}}
         │
         ▼
{{子系统回调}} ({{file.c:line}})
│  {{检查函数}}({{capability}})  ← {{命名空间感知 / 仅init_user_ns}}
│  失败 → {{-EPERM / 丢弃}}
         │
         ▼
{{漏洞代码路径}}
```

**有效权限要求**: {{链中最严格的检查}}
- {{例如："需要真实root——回调在X行使用capable()"}}
- {{或"通过userns可无特权触发——所有检查使用ns_capable()"}}

**注意**: {{关于未来可能扩大攻击面的变更的评论}}

### 5.3 原语分析

- **漏洞提供的能力**: {{例如：256字节slab对象上的UAF}}
- **可控性**: {{攻击者有多少控制权？}}
- **时序**: {{竞态窗口是否足够宽？}}

### 5.4 根因路径穷举

不要将分析局限于PoC崩溃的单一路径。根因——底层的不变量违规——可能在多个代码位置显现，
每个位置产生不同的利用原语。

**根因模式**: {{精确描述不变量违规，而非"函数X中的崩溃"}}

#### 所有受影响的代码位置

| # | 函数 (行号) | 上下文 | 是否有保护？ | 是否存在漏洞？ |
|---|---|---|---|---|
| 1 | `{{func_a()}}` L{{NNN}} | {{工作队列 / 软中断 / 进程}} | {{否}} | **是** |
| 2 | `{{func_b()}}` L{{NNN}} | {{进程上下文}} | {{是 (kref_get_unless_zero)}} | 否 |
| ... | ... | ... | ... | ... |

#### 逐路径原语分析

**路径 {{N}}: `{{func}}()` ({{上下文}})**
- 漏洞访问后的操作: {{代码对该对象做了什么}}
- 原语: {{DoS / 信息泄露 / UAF / 任意写 / 代码执行}}
- 竞态窗口: {{宽度，能否通过userfaultfd/FUSE/CPU绑核扩大？}}
- 喷射目标: {{slab缓存、对象大小、可行性}}
- 崩溃前的时间窗口（销毁时序）: {{T1: put/free → T2: ptr=NULL → T3: kfree_rcu。攻击者命中哪个窗口？}}

{{对每条漏洞路径重复}}

#### 所有路径中的最强原语

{{最危险的路径是#N，因为... / 所有路径都仅为DoS，因为...}}

### 5.5 替代利用分析

系统性地质疑初始评估，深入探究。

**考虑过的挑战**:
1. 是否为伪装成NULL解引用的UAF？ — {{是/否：引用具体的锁/销毁序列分析}}
2. 出错偏移/地址是否可控？ — {{是/否：追踪偏移来源}}
3. 不同时序是否产生更强的原语（NULL前窗口 vs NULL后窗口）？ — {{是/否：逐步分析销毁序列}}
4. 不同代码路径是否能获得写而非读？ — {{是/否：引用上方路径穷举}}
5. 能否与其他漏洞链式利用（信息泄露+本漏洞）？ — {{是/否：推理}}
6. 不开启CONFIG_INIT_ON_FREE / KASAN——行为是否改变？ — {{是/否：推理}}
7. userfaultfd/FUSE能否扩大竞态窗口？ — {{是/否：推理}}

**结论**: {{初始评估成立 / 升级为X，因为路径#N提供了Y原语}}

### 5.6 利用路径

{{如可利用，描述理论利用路径：}}

1. {{步骤1：例如，通过...触发UAF}}
2. {{步骤2：例如，使用msg_msg喷射在kmalloc-256中回收...}}
3. {{步骤3：例如，通过...泄露内核指针}}
4. {{步骤4：例如，覆写函数指针实现代码执行...}}

### 5.7 缓解措施

| 缓解措施 | 适用？ | 可绕过？ |
|---|---|---|
| KASLR | {{是/否}} | {{是——通过信息泄露 / 否}} |
| SMEP | {{是/否}} | {{是——通过ROP / 否}} |
| SMAP | {{是/否}} | {{是/否}} |
| CFI | {{是/否}} | {{是/否}} |
| Slab加固 | {{是/否}} | {{是/否}} |

### 5.8 先例

{{来自kernelCTF或公开利用的类似漏洞：}}
- {{CVE-XXXX-XXXX}}: {{子系统}}中类似的{{漏洞类型}}，通过{{技术}}利用

---

## 6. 补丁

### 6.1 修复描述

{{补丁做了什么以及为什么它正确修复了根因。}}

### 6.2 补丁Diff

```diff
{{实际补丁diff}}
```

### 6.3 提交信息

提交信息必须包含解码后的回溯（来自 `scripts/decode_stacktrace.sh`）。

```
{{subsystem: brief description}}

{{根因说明。}}

{{为什么此补丁能修复。}}

Decoded backtrace:

 {{BUG: KASAN: ... in function_name (file.c:line)}}
 {{Call Trace:}}
  {{<IRQ>}}
  {{crash_function (file.c:line)}}
  {{caller_function (file.c:line)}}
  {{...裁剪到关键帧...}}
  {{</IRQ>}}

Fixes: {{hash}} ("{{commit title}}")
Reported-by: {{name}} <{{email}}>
Cc: stable@vger.kernel.org
Signed-off-by: {{name}} <{{email}}>
```

### 6.4 验证

- [ ] `checkpatch.pl --strict` 通过
- [ ] 使用defconfig编译通过
- [ ] 使用相关调试配置编译通过（KASAN等）
- [ ] 启动测试通过
- [ ] PoC不再崩溃
- [ ] 无新的KASAN/UBSAN警告
- [ ] 子系统自测通过（如适用）
- [ ] 基本功能无回归

---

## 7. 验证结果

### 7.1 漏洞内核测试

```
{{在漏洞内核上运行PoC时显示崩溃的dmesg输出}}
```

### 7.2 修复内核测试

```
{{在修复内核上运行PoC时显示正常运行的dmesg输出}}
```

### 7.3 压力测试

{{在修复内核上循环运行PoC的结果}}

---

## 8. 产出物

| 产出物 | 路径 | 描述 |
|---|---|---|
| PoC源码 | `poc/poc.c` | 概念验证代码 |
| PoC二进制 | `poc/poc_binary` | 编译后的PoC |
| 补丁 | `patch/0001-*.patch` | git format-patch输出 |
| 漏洞内核 | `kernel/test-bzImage` | 可启动的漏洞内核 |
| 漏洞vmlinux | `kernel/test-vmlinux` | 调试符号 |
| 修复内核 | `kernel/patched-bzImage` | 可启动的修复内核 |
| 修复vmlinux | `kernel/patched-vmlinux` | 调试符号 |
| 内核配置 | `kernel/.config` | 构建配置 |
| 根文件系统 | `env/rootfs.cpio.gz` | QEMU根文件系统 |
| 启动脚本(漏洞) | `env/run-vulnerable.sh` | 启动漏洞内核 |
| 启动脚本(修复) | `env/run-patched.sh` | 启动修复内核 |
| 启动脚本(调试) | `env/run-debug.sh` | 启动GDB调试 |
| 崩溃日志 | `logs/crash.log` | 原始崩溃日志 |
| 验证日志 | `logs/patch-verification.log` | 补丁测试输出 |

---

## 9. 参考

- {{原始漏洞报告 / syzbot / 邮件列表链接}}
- {{相关内核源码链接}}
- {{相关CVE}}
- {{类似漏洞的kernelCTF参考}}
- {{相关内核文档}}

---

## 10. 环境与可复现性

记录精确环境以确保可复现性：

| 项目 | 值 |
|---|---|
| 内核源码 | `{{git repo URL}}` @ `{{commit hash}}` |
| 内核版本 | `{{make kernelversion 输出}}` |
| 编译器 | `{{gcc --version / clang --version}}` |
| QEMU | `{{qemu-system-x86_64 --version}}` |
| Busybox | `{{busybox --help | head -1}}` |
| 宿主机OS | `{{uname -a}}` |

---

## 11. 时间线

| 日期 | 事件 |
|---|---|
| {{date}} | 漏洞报告 / 观察到崩溃 |
| {{date}} | 确认根因 |
| {{date}} | 开发补丁 |
| {{date}} | 验证补丁 |
| {{date}} | {{提交上游 / 等}} |
